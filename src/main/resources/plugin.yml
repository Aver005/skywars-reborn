name: SkyWars
main: ru.kiviuly.skywars.SkyWarsPlugin
version: '${version}'
api-version: '1.21'
author: kiviuly
description: SkyWars minigame (arenas, capsule start, kits, damage-race matches).
depend: [MgCore]
# Команд своих нет: арены и вход — через /mg (ядро), игро-подкоманды (kits/loot/chests)
# — через MgCore-делегирование в SkyWarsGame.onCommand. Права — mg.admin (ядра).
